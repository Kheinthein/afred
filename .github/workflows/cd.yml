name: CD - Deployment

on:
  push:
    branches: [main]
    tags:
      - 'v*'

jobs:
  deploy:
    name: Deploy to Production
    runs-on: ubuntu-latest
    if: github.ref == 'refs/heads/main'

    steps:
      - name: 📥 Checkout code
        uses: actions/checkout@v4

      - name: 🔧 Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: '20.x'

      - name: 📦 Install dependencies
        run: npm ci

      - name: 🏗️ Build
        run: npm run build
        env:
          DATABASE_URL: ${{ secrets.DATABASE_URL }}
          JWT_SECRET: ${{ secrets.JWT_SECRET }}
          ANTHROPIC_API_KEY: ${{ secrets.ANTHROPIC_API_KEY }}

      - name: 🧪 Run tests
        run: npm test
        env:
          DATABASE_URL: file:./test.db
          JWT_SECRET: test-secret
          NODE_ENV: test

      - name: 🐳 Build Docker image
        run: |
          docker build -f docker/Dockerfile -t alfred-app:${{ github.sha }} .
          docker tag alfred-app:${{ github.sha }} alfred-app:latest

      - name: 📤 Push to registry
        run: |
          echo "${{ secrets.DOCKER_PASSWORD }}" | docker login -u "${{ secrets.DOCKER_USERNAME }}" --password-stdin
          docker push alfred-app:${{ github.sha }}
          docker push alfred-app:latest

      - name: 🚀 Deploy
        run: |
          echo "Deployment step - configure based on your infrastructure"
          # Add your deployment commands here
          # Examples:
          # - kubectl apply -f k8s/
          # - scp docker-compose.yml user@server:/app/
          # - ssh user@server "cd /app && docker-compose up -d"

